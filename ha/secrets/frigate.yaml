# PLACEHOLDER — NOT YET ENCRYPTED.
#
# Replace this file with a sops-encrypted version before deploying, or the
# host will fail to decrypt at activation. Steps (see ha/secrets/README.md):
#   1. Put your admin + host age pubkeys in ../../.sops.yaml
#   2. From the repo root:  sops --encrypt --in-place ha/secrets/frigate.yaml
#      (or: rm this file, then `sops ha/secrets/frigate.yaml` and add the keys)
#   3. Edit anytime with:   sops ha/secrets/frigate.yaml
#
# Keys consumed by configuration.nix (sops.secrets.*):
frigate_rtsp_user: REPLACE_ME
frigate_rtsp_password: REPLACE_ME
