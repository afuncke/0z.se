# PLACEHOLDER — not yet encrypted. Replace the value with the real Anthropic
# API key and encrypt in place before deploying:
#
#   sops ha/secrets/llm-proxy.yaml          # edit + auto-encrypt (recipients from ../../.sops.yaml)
#   # or, if editing the plaintext directly:
#   sops --encrypt --in-place ha/secrets/llm-proxy.yaml
#
# The key name must stay `llm_anthropic_api_key` (configuration.nix references
# it). sops.validateSopsFiles is off, so the repo still evaluates with this
# plaintext placeholder; activation on the host will fail until it's encrypted.
llm_anthropic_api_key: REPLACE_ME_WITH_REAL_ANTHROPIC_API_KEY
